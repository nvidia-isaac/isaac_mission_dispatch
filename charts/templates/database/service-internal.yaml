{{- if .Values.enableChart -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mission-dispatch.fullname" . }}-database-internal
  labels:
    app: {{ include "mission-dispatch.fullname" . }}-database
spec:
  ports:
    - port: 80
      targetPort: http-internal
      protocol: TCP
      name: http
  selector:
    app: {{ include "mission-dispatch.fullname" . }}-database
{{- end }}